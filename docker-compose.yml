services:
  account:
    build: .
    container_name: yyds-account-center
    restart: unless-stopped
    ports:
      # 3000=www 3001=xiaowenhua 3002=zhouyuding — do not reuse those
      - "3003:3000"
    env_file:
      - .env.production
    environment:
      DATABASE_URL: file:/data/prod.db
    volumes:
      - account-data:/data
      - account-uploads:/app/public/uploads

volumes:
  account-data:
  account-uploads:
